# COS 710 Assignment 1 — Genetic Programming
Run with `java -jar Assignment1.jar` (default seed: 10) or `java -jar Assignment1.jar <seed>` to specify a custom seed (e.g. `java -jar Assignment1.jar 42`).
